# Copy this to .env and fill in values

# MongoDB connection (only needed for contact form persistence)
MONGO_URL=mongodb://localhost:27017
DB_NAME=portfolio

# Base URL for the app (used for absolute links in metadata if you need it)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
